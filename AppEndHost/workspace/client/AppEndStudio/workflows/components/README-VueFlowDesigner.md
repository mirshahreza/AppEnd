# Vue Flow Workflow Designer - راهنمای نصب و استفاده

## ⚠️ وضعیت فعلی

دیزاینر Vue Flow نیاز به نصب پکیج‌های اضافی دارد. در حال حاضر وقتی روی دکمه "Designer" کلیک می‌کنید، یک صفحه راهنما برای نصب نمایش داده می‌شود.

## 📦 نصب Vue Flow

### مرحله 1: نصب پکیج‌ها

در پوشه اصلی پروژه (جایی که `package.json` وجود دارد)، این دستور را اجرا کنید:

```bash
npm install @vue-flow/core @vue-flow/background @vue-flow/controls @vue-flow/minimap
```

یا با Yarn:

```bash
yarn add @vue-flow/core @vue-flow/background @vue-flow/controls @vue-flow/minimap
```

### مرحله 2: Refresh مرورگر

بعد از نصب، مرورگر را Refresh کنید (F5 یا Ctrl+R).

### مرحله 3: استفاده از دیزاینر

پس از نصب، وقتی روی دکمه "Designer" کلیک می‌کنید، دیزاینر گرافیکی Vue Flow باز می‌شود.

## 🎯 گزینه‌های جایگزین (بدون نیاز به نصب)

اگر نمی‌خواهید Vue Flow را نصب کنید، می‌توانید از این گزینه‌ها استفاده کنید:

1. **Legacy Designer** - دیزاینر قدیمی داخلی (دکمه خاکستری)
2. **Elsa Studio** - دیزاینر Elsa تعبیه‌شده (دکمه آبی روشن)

## 🚀 ویژگی‌های Vue Flow Designer (پس از نصب)

- ✨ رابط کاربری Drag & Drop حرفه‌ای
- 🎨 Activity Palette با دسته‌بندی
- ⚙️ Properties Panel برای ویرایش
- 🗺️ Minimap برای نمای کلی
- 🔍 Zoom Controls پیشرفته
- 📐 Auto Layout هوشمند
- 🔗 اتصالات گرافیکی بصری
- 💾 ذخیره‌سازی خودکار در فرمت Elsa

## 📚 منابع

- **Vue Flow Documentation**: https://vueflow.dev/
- **GitHub Repository**: https://github.com/bcakmakoglu/vue-flow
- **Examples**: https://vueflow.dev/examples/

## 🐛 عیب‌یابی

### خطا: "Cannot find module '@vue-flow/core'"

**راه‌حل**: پکیج‌های Vue Flow را نصب کنید:
```bash
npm install @vue-flow/core @vue-flow/background @vue-flow/controls @vue-flow/minimap
```

### دیزاینر باز نمی‌شود

**راه‌حل**:
1. مطمئن شوید پکیج‌ها نصب شده‌اند
2. Browser Cache را پاک کنید (Ctrl+Shift+Delete)
3. مرورگر را Refresh کنید (F5)
4. پروژه را Rebuild کنید

### پیام "Installation Required" نمایش داده می‌شود

این طبیعی است. پکیج‌ها را نصب کنید و مرورگر را Refresh کنید.

## 💡 توصیه

برای بهترین تجربه، توصیه می‌کنیم Vue Flow را نصب کنید. این یک کتابخانه رایگان و Open Source است که توسط هزاران پروژه استفاده می‌شود.

اگر نمی‌خواهید آن را نصب کنید، می‌توانید از دیزاینرهای دیگر (Legacy یا Elsa Studio) استفاده کنید.

---

**آخرین بروزرسانی**: این راهنما در زمان ساخت اولیه دیزاینر نوشته شده است.
