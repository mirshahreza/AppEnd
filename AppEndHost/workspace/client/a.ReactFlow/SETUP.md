# 🚀 نحوه راه‌اندازی ReactFlow Designer

## مرحله 1: نصب Dependencies

```bash
cd AppEndHost/workspace/client/a.ReactFlow
npm install
```

اگر `npm` نصب نیست:
- Windows: دانلود از https://nodejs.org/ (LTS)
- بعد از نصب، PowerShell را دوباره باز کنید

## مرحله 2: Build React App

```bash
npm run build
```

این فرمان:
- React app را compile می‌کند
- تمام فایل‌ها را minimize می‌کند
- خروجی را در `dist/` فولدر قرار می‌دهد

✅ اگر موفق بود، میبینید:
```
✓ built in 2.34s
```

## مرحله 3: راه‌اندازی Server

بازگردید به root پروژه و server را start کنید:
```bash
# در Visual Studio: Ctrl+F5 یا Debug > Start without Debugging
# یا از command line:
dotnet run --project AppEndHost/AppEndHost.csproj
```

## مرحله 4: تست

1. مرورگر را باز کنید: `https://localhost:7000` (یا پورت شما)
2. به Workflow Manager بروید
3. روی دکمه "Flow" کلیک کنید
4. ReactFlow Designer باید باز شود

## 🔍 اگر کار نکرد:

### مشکل 1: "Cannot GET /a.ReactFlow/dist/index.html"
**علت**: Build نشده است
**حل**:
```bash
cd AppEndHost/workspace/client/a.ReactFlow
npm run build
```

### مشکل 2: npm نصب نیست
**حل**: https://nodejs.org/ دانلود و نصب کنید

### مشکل 3: npm command not found
**حل**: PowerShell را به عنوان Admin باز کنید

### مشکل 4: صفحه سفید بدون خطا
**حل**: Browser console (F12) باز کنید و errors را بررسی کنید

---

## Development Mode (اختیاری)

برای تغییرات سریع بدون rebuild:

```bash
cd AppEndHost/workspace/client/a.ReactFlow
npm run dev
```

سپس URL را تغییر دهید:
```javascript
// در ReactFlowDesigner.vue:
const baseUrl = "http://localhost:5173/src/App.jsx";
```

⚠️ فقط برای development - برای production باید build شود!

---

## فرمان‌های مفید

```bash
# Build اضطراری (clean build)
npm run build

# Preview output قبل از deployment
npm run preview

# Install specific version
npm install reactflow@latest
```

---

## فایل ساختار پس از build

```
a.ReactFlow/
├── dist/                    # 👈 Generated by "npm run build"
│   ├── index.html
│   ├── app.js              # Main bundled JS
│   └── ...other files
├── src/                    # Source files
├── node_modules/           # Dependencies
└── package.json
```

ASP.NET Core میدهنگ `/a.ReactFlow/dist/` را serve می‌کند ✅

